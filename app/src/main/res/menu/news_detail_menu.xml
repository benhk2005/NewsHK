<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/downloadActionButton"
        android:icon="@drawable/ic_action_cloud_download"
        android:title="@string/download_for_offline"
        app:showAsAction="always"/>

    <item
        android:id="@+id/downloadedDoneActionButton"
        android:icon="@drawable/ic_action_cloud_done"
        android:title="@string/news_has_been_downloaded"
        app:showAsAction="always"
        android:visible="false"/>

    <item
        android:id="@+id/shareActionButton"
        android:icon="@drawable/ic_action_share"
        android:title="@string/share"
        app:showAsAction="always"/>
</menu>